import { UserRound } from 'lucide-react';import { Link } from 'react-router-dom';import { Badge,PageHeader } from '../components/ui';import { useDemo } from '../state/DemoContext'
export default function TrainingProfiles(){const {profiles}=useDemo();return <><PageHeader eyebrow="Crew Development" title="Training profiles" description="Link training-sortie and assessment observations to individual profiles so trainers and checkers can track development over time."/><div className="grid gap-4 xl:grid-cols-3">{profiles.map(p=><Link key={p.id} to={`/training-profiles/${p.id}`} className="card group p-4 transition hover:-translate-y-0.5 hover:border-accent/30"><div className="flex items-center gap-3"><div className="grid h-10 w-10 place-items-center rounded-xl bg-purple/10 text-purple"><UserRound size={18}/></div><div><div className="text-sm font-semibold text-ink group-hover:text-accent">{p.name}</div><div className="mt-1 text-[10px] text-faint">{p.role} · {p.base}</div></div></div><div className="mt-4 space-y-2">{p.skills.map(s=><div key={s.name}><div className="mb-1 flex justify-between gap-3 text-[9px]"><span className="text-muted">{s.name}</span><span className="font-semibold text-ink">{s.score}</span></div><div className="h-1.5 overflow-hidden rounded-full bg-panel"><div className={`h-full rounded-full ${s.trend==='Watch'?'bg-accent':'bg-success'}`} style={{width:`${s.score}%`}}/></div></div>)}</div><div className="mt-4 flex items-center justify-between"><Badge tone="muted">{p.debriefIds.length} linked records</Badge><span className="text-[9px] text-faint">Last assessment {p.lastAssessment}</span></div></Link>)}</div></>}
