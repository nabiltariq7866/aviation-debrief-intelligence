import { Search } from 'lucide-react';import { useMemo,useState } from 'react';import { Link } from 'react-router-dom';import { Badge,PageHeader } from '../components/ui';import { useDemo } from '../state/DemoContext'
export default function Lessons(){const {lessons}=useDemo();const [q,setQ]=useState('');const visible=useMemo(()=>lessons.filter(l=>`${l.title} ${l.summary} ${l.category}`.toLowerCase().includes(q.toLowerCase())),[lessons,q]);return <><PageHeader eyebrow="Lessons Learned" title="Lessons learned intelligence" description="AI extracts candidate lessons, links supporting debriefs and prepares knowledge for human validation and wider distribution."/><div className="mb-4 relative"><Search size={15} className="absolute left-3 top-1/2 -translate-y-1/2 text-faint"/><input className="field h-11 pl-9" value={q} onChange={e=>setQ(e.target.value)} placeholder="Search lessons, categories or themes…"/></div><div className="grid gap-3 xl:grid-cols-2">{visible.map(l=><Link to={`/lessons/${l.id}`} key={l.id} className="card group p-4 transition hover:-translate-y-0.5 hover:border-accent/30"><div className="flex items-start justify-between gap-3"><div><div className="text-sm font-semibold text-ink group-hover:text-accent">{l.title}</div><div className="mt-1 text-[10px] text-faint">{l.id} · {l.category}</div></div><Badge tone={l.status==='Published'?'success':'accent'}>{l.status}</Badge></div><div className="mt-3 text-xs leading-5 text-muted">{l.summary}</div><div className="mt-4 grid grid-cols-3 gap-3"><div><div className="text-[9px] text-faint">Occurrences</div><div className="mt-1 text-base font-semibold text-ink">{l.occurrences}</div></div><div><div className="text-[9px] text-faint">Crews</div><div className="mt-1 text-base font-semibold text-ink">{l.crews}</div></div><div><div className="text-[9px] text-faint">Confidence</div><div className="mt-1 text-base font-semibold text-accent">{l.confidence}%</div></div></div></Link>)}</div></>}
